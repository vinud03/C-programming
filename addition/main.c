#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b,sum;
    printf("Enter Two numbers");
    scanf("%d %d",&a,&b);
    sum=a+b;
    printf("Addition of two numbers is:%d",sum);
    return 0;
}
